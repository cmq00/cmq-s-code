#include <bits/stdc++.h>

using namespace std;

const int N = 1e6 + 4;

int main()
{
    int a[N];
    int n;
    scanf("%d",&n);
    for(int i = 1;i <= n;++i)
        scanf("%d",&a[i]);
    int ans = 0;
    int l = n / 2,r = n / 2 + 1;
    while(l)
    printf("%d\n",ans);
    return 0;
}