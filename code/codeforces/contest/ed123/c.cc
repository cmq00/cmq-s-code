#include <bits/stdc++.h>
#define ll long long
using namespace std;
 
int a[50004];
int z[50004];
int f[50004];

void solve()
{
    int n;
    cin >> n;
    int maxx = -100004;
    int minx = 100004;
    for(int i = 1;i <= n;++i){
        cin >> a[i];
        if(a[i])
    }
}
 
int main(){
    ios_base::sync_with_stdio(0);
    int T = 1;//*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}