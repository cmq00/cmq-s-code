#include <bits/stdc++.h>
#define ll long long
#define ms(i) memset(i, 0, sizeof(i))
 
using namespace std;

const int N = 1e5 + 4;

struct Node
{
    int id;

}a[N];

void solve()
{
    int n,m,x;
    cin >> n >> m >> x;
    for(int i = 1;i <= n;++i){
        cin >> a[i];
    }

}

int main()
{
    //ios_base::sync_with_stdio(0);
    int T = 1;//*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}'