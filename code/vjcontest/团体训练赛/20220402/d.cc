#include <bits/stdc++.h>
#define ll long long
using namespace std;

void solve()
{   
    int b[4];
    int d[4];
    int ans = 0;
    for(int i = 1;i <= 3;++i)
        cin >> b[i];
    for(int i = 1;i <= 3;++i)
        cin >> d[i];
    for(int i = 1;i <= 3;++i){
        
    }
}

int main()
{
    //ios_base::sync_with_stdio(0);
    int T = 1;//*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}