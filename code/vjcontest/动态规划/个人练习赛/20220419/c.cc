#include <bits/stdc++.h>
#define ll long long
#define ms(i) memset(i, 0, sizeof(i))
 
using namespace std;

bool check(char ch)
{
    if(ch != 'a' && ch != 'b' && ch != 'c' && ch != '*' && ch != '/')
        return 0;
    return 1;
}

void solve()
{
    
}

int main()
{
    //ios_base::sync_with_stdio(0);
    int T = 1;/*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}