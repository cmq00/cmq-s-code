#include <bits/stdc++.h>
#include <iostream>

#define ll long long
#define ms(i) memset(i, 0, sizeof(i))
 
using namespace std;

const int N = 1e6 + 4;
ll a[N];

void solve()
{
    int n,m;
    cin >> n;
    for(int i = 1;i <= n;++i){
        cin >> a[i];
    }
    cin >> m;

}

int main()
{
    //ios_base::sync_with_stdio(0);
    int T = 1;//*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}