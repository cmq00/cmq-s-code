#include <bits/stdc++.h>
#define ll long long
using namespace std;

void solve()
{
    char a[100][100];
    int n,m;
    cin >> n >> m;
    for(int i = 1;i <= n;++i){
        scanf("%s",a[i]);

    }
}

int main()
{
    //ios_base::sync_with_stdio(0);
    int T = 1;//*
    cin >> T;//*/
    while(T--) solve();
    return 0;
}